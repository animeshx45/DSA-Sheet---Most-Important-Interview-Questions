[Approach -- Useing Inbuild JAVA pow Functions in the Math Libraries]
Time Complexity -- O(log b)

class Solution {
    public double myPow(double x, int n) {
        double ans = Math.pow(x,n);
        return ans;
        
    }
}
